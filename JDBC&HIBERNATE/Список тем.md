- **JDBC: подключение к БД, Statement, PreparedStatement, ResultSet**
- **ORM-концепция: что такое ORM, зачем нужен Hibernate**
- **Entity, аннотации JPA: @Entity, @Table, @Column, @Id, @GeneratedValue**
- **Связи между сущностями: @OneToOne, @OneToMany, @ManyToMany**
- **Lazy vs Eager загрузка, проблема N+1**
- **HQL / JPQL — запросы к базе через объектную модель**
- **SessionFactory, EntityManager, жизненный цикл сущности**
- **Транзакции в Hibernate + Spring: @Transactional под капотом**