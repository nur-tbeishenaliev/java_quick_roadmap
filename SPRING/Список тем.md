- **Spring Core: IoC контейнер, бины, ApplicationContext**
- **Dependency Injection (DI): @Autowired, @Component, @Service, @Repository**
- **Spring Boot: автоконфигурация, application.properties, структура проекта**
- **Spring MVC: контроллеры, @RestController, @RequestMapping, маршруты**
- **Обработка запросов: @GetMapping, @PostMapping, @RequestBody, @PathVariable**
- **Spring Data JPA: репозитории, CrudRepository, JpaRepository, методы по имени**
- **Транзакции в Spring: @Transactional, propagation, rollback**
- **Spring Security: базовая настройка, аутентификация, авторизация, роли**
- **JWT-аутентификация в Spring Boot**
- **Валидация данных: @Valid, @NotNull, @Size, BindingResult, обработка ошибок**